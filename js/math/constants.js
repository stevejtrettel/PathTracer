// The two coordinate-system constants Matrix4 imports (verbatim values from
// three's src/constants.js). Copied here so the local math cluster is
// self-contained and doesn't pull in three.
export const WebGLCoordinateSystem = 2000;
export const WebGPUCoordinateSystem = 2001;
